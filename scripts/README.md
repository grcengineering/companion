# Scripts

Planned scripts:

- `ingest-newsletters` - convert newsletter exports into structured corpus files.
- `ingest-linkedin` - convert LinkedIn JSONL into structured corpus files.
- `build-adapters` - emit platform-specific bundles from canonical source files.

Sprint 1 keeps these as placeholders until source locations and target adapter formats are confirmed.
