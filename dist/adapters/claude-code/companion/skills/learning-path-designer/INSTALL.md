# Install: learning-path-designer

Install by copying this directory into a supported adapter bundle.

For Claude-style skill installs:

```bash
mkdir -p ~/.claude/skills
cp -R skills/learning-path-designer ~/.claude/skills/learning-path-designer
```

Then run the checks in `VERIFY.md`.
