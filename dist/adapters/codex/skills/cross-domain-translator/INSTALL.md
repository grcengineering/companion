# Install

Install this skill by packaging it through `scripts/build-adapters`.

The skill is invoked invisibly as a supporting move when a cross-domain pattern would help the learner, or explicitly through `/grc-companion:translate`.
